# Copy to .env for local development (never commit the real .env).
# Get this from the Render dashboard: your Postgres instance → Connections
# → Internal Database URL (use External Database URL if connecting from
# outside Render, e.g. from your laptop).
DATABASE_URL=postgresql://user:password@host:5432/dbname
