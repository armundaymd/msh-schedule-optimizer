services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: msh
      POSTGRES_PASSWORD: msh
      POSTGRES_DB: msh_schedule_optimizer
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
